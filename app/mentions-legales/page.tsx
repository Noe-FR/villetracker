import { MentionsLegales } from "@/src/views/MentionsLegales";

export const metadata = { title: "Mentions légales — VilleTracker" };

export default function Page() {
  return <MentionsLegales />;
}
